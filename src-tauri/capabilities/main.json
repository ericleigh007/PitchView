{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow the main window to access core APIs and open the native media picker.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}